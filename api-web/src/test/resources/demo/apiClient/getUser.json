{
  "request": {
    "method": "get",
    "url": "/getUser",
    "name": "获取用户信息",
    "param": {
      "name": "${username}"
    },
    "headers": {
      "helloWorld": "the world!!!",
      "Cookie": [
        {
          "JSESSIONID": "${sessionId}",
          "hi": "hello"
        }
      ]
    }
  },
  "response": {
    "code": 200,
    "data": "${random}"
  }
}